name: Build and Deploy Blog

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build Blog
        run: node src/index.js

      - name: Deploy to GitHub Pages
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          git checkout main
          rm -rf 2026 archives css img js atom.xml index.html || true
          
          cp -r build/* ./
          
          git add -A
          git diff --quiet && git diff --staged --quiet || git commit -m "Deploy blog"
          git push origin main
