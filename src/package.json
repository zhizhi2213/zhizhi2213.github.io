{
  "name": "minimal-blog-generator",
  "version": "1.0.0",
  "description": "A modern and elegant static blog generator",
  "main": "src/index.js",
  "scripts": {
    "build": "node src/index.js",
    "serve": "node -e \"require('http').createServer((req, res) => { require('fs').readFile(__dirname + (req.url === '/' ? '/index.html' : req.url), (err, data) => { if (err) { res.writeHead(404); res.end('Not Found'); } else { res.writeHead(200); res.end(data); } }); }).listen(4000, () => console.log('Server running at http://localhost:4000'))\""
  },
  "keywords": ["blog", "static-site-generator", "markdown"],
  "author": "zhizhi2213",
  "license": "MIT",
  "dependencies": {
    "markdown-it": "^14.1.0",
    "markdown-it-highlightjs": "^4.0.1",
    "markdown-it-anchor": "^9.2.0",
    "markdown-it-task-lists": "^2.1.1",
    "highlight.js": "^11.10.0"
  }
}
